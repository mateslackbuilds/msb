This directory contains the required base packages for the MATE desktop
environment.  The script "mate-build-base.sh" in the root directory will 
build and install all the packages in the correct order.
